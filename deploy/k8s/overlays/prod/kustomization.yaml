apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tts-hotkey-prod

resources:
  - ../../base

images:
  - name: ghcr.io/erikalira/tts-hotkey-windows-bot
    newTag: v0.0.0

patches:
  - path: replica-patch.yaml
  - path: resources-patch.yaml
