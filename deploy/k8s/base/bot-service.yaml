apiVersion: v1
kind: Service
metadata:
  name: bot
  labels:
    app.kubernetes.io/name: tts-hotkey-windows
    app.kubernetes.io/component: bot
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tts-hotkey-windows
    app.kubernetes.io/component: bot
  ports:
    - name: http
      port: 10000
      targetPort: http
